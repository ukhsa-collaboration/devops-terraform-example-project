name: "Plan Terraform stacks in Development"
on:
  pull_request:
    branches:
      - main

jobs:
  plan:
    name: Plan Development Terraform
    uses: UKHSA-Internal/devops-github-reusable-workflows/.github/workflows/terraform-plan-apply.yml@v0.11.0
    permissions:
      packages: read
      contents: read
      security-events: write
      statuses: write
      checks: write
      id-token: write
    with:
      environment_name: dev
      execute_terraform_plan: false
    secrets:
      AWS_ACCOUNT_ID: "${{ secrets.AWS_ACCOUNT_ID_DEV }}"
      AWS_ROLE_NAME: "${{ secrets.AWS_ROLE_NAME }}" 